#![forbid(unsafe_code)]

// Inert research target. This package exists only to make Cargo resolve the
// exact Specification 004P dependency graph; it is never a Himsat product crate.
pub const RESEARCH_ONLY: bool = true;
